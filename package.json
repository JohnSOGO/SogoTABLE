{
  "name": "sogotable",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy:brain": "wrangler deploy --config wrangler.toml"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
