{
  "name": "sogotable",
  "version": "0.1.2",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy:brain": "npx wrangler deploy --config wrangler.toml",
    "test:worker": "node --test workers/tests/*.test.js"
  },
  "devDependencies": {
    "playwright": "^1.61.0",
    "wrangler": "^4.0.0"
  }
}
