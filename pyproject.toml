[project]
name = "sogotable"
version = "0.1.0"
description = "Local-first family turn-based game platform."
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
